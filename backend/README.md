# Annpurna Scanner — Backend

Simple Node/Express backend using Mongoose and dotenv.

Run locally:

1. cd backend
2. npm install
3. copy `.env` and update `MONGO_URI`
4. npm run dev

API endpoints:
- GET `/api/sample`
- POST `/api/sample`
